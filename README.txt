====================
django-join-requests
====================

django-join-requests is a pluggable app, intended to allow users to
request access to some resource or resources (such as a game), and for
a privileged user to accept or deny the request.

Requirements
------------
Python >= 2.6
Django >= 1.2
